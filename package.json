{
  "name": "desorden-scrollytelling-worker",
  "version": "4.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.mjs"
  },
  "devDependencies": {
    "wrangler": "4.115.0"
  }
}
